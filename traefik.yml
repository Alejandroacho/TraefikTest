# API and dashboard configuration
api:
  # Dashboard
  dashboard: true
  insecure: true

# Docker configuration backend
providers:
  docker:
    watch: true
    exposedByDefault: true

# Traefik Logging
log:
  level: INFO